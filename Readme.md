// Disable timestamps for better performance
timestamps: false,
// Use a specific table name
tableName: 'users',
// Disable automatic pluralization
freezeTableName: true,
// Disable model name modification
underscored: true,
// Use raw insert for better performance
returning: false,
